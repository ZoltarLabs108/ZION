# PHASE: RED DAWN — RECIPE STEP 1 · 1c–1g (discovery)
Tier:    AUTO   (the core model)
Worktree: ZION_RED_DAWN   Branch: reddawn-dev   (pre-existing)
Owns:    ZION calc-5b/5c (fit, tier) — the four-model cascade within each 27-CAPE-type

## CONTRACT (the handoff)
IN   ← HYPERION: the feature/panel matrix + fold split (Stage-2 walk-forward).
OUT  → conditioning/voice phases (INTERSTELLAR, VALUATION, EMISSION, ODYSSEY, SANCTUARY) and
       DECISION: the certified 27-type cascade signal per month + tier level + IS→OOS funnel.
GATE :  rounds proceed to the vaLB-floor (first-breach); tier wf-OOS column is a SELECTION stat,
        not a gate; every funnel zero must be attributable.

## ACTIONS  (in order)
1. **27-type sign grammar** — ternary triple Δ(A), Δ(B), Δ(zA−zB) → 27 types (`grammar27`,
   ±0.5% dead zone). `classify_cape_type`.
2. **Four-model cascade WITHIN each type** — primary / secondary / model-3 / model-4 threshold
   partition, each with a Wilson one-sided flip; train-MAJORITY-direction + Wilson-LB gate.
3. **Recurse** rounds to the vaLB-floor (per-asset floor, default 0.50; short-history e.g. Silver
   0.45), first-breach stop.
4. **1c tier level** — wf-OOS as selection stat.
5. **1f IS→OOS funnel** — every zero attributable (invariant-enforced).
6. **1g lifecycle** — dormancy / retire logged.

## DIAGNOSTICS
- pass criterion: cascade rounds terminate at floor; tier + funnel columns emitted; N/N auditor PASS.
- emitted: `stage_STEP1_cascade`, `_27type_grammar`, `stage_STEP1c_tier`, `stage_STEP1f_funnel`,
  `stage_STEP1g_lifecycle`.
- failure mode → cascade-empty is a VALID output (asset abstains at DECISION); VALUATION still runs.

## CADENCE
- MONTHLY : F — full 27-type recursive discovery.
- WEEKLY  : s — re-SCORE on frozen tiers only (NO re-discovery — re-discovery weekly overfits).
- DAILY   : N/A — discovery is monthly.

## PROVENANCE / PRIOR ART  (load-bearing)
- **Contamination boundary — do NOT reproduce:** HYACINTH_X full-history fit of thresholds/flips/
  accuracies (`analyze_type`, `cape_type_accuracy`); port fixed blocks U[:50]/[50:75]/[75:],
  split=0.65 tail; any floor tuned after seeing TEST. No true purged WF existed in X (WF/perm gates
  were dead code, RED_DAWN 2482–2488). **ZION Stage-2/3 is the ecosystem's first real walk-forward.**
- KEEP: 27-type grammar + train-majority-direction + Wilson-LB gate (lib_pipeline 399–442).
  REPLACE: in-sample fit → walk-forward folds. [[cyclops-walkforward-pilot-gold]] (>70% IS tiers →
  ~54% OOS; conviction gradient inverted — do not oversell tier conviction).
